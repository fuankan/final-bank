<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kg.fkapps.finalbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".allUsers.AllUsersActivity" />
        <activity
            android:name=".transaction.TransactionHistoryActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SingleUserActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".AddUser" />
        <activity
            android:name=".selectUser.SelectUsersActivity" />
    </application>

</manifest>